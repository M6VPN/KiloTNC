# Embedded Common

This directory is reserved for embedded-only helper code if M2 later needs it.

No shared helper code is added in M2.0. Portable protocol, modem, diagnostics, and TNC logic remain in `firmware/`.
